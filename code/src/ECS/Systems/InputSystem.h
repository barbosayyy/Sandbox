#pragma once

#include "Core/Collections.h"
#include "ECS/Registry.h"

namespace Sb {
    namespace ECS {
        class InputSystem {
        public:
            static void Update(Registry& registry) {
            }
        };
    }
}